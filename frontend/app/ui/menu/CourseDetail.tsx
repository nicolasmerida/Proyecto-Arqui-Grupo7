// app/ui/menu/CourseDetail.tsx
import { Plato } from "@/app/lib/definitions";

export default function CourseDetail({course} : { course: Plato }) {

    return (
        <div className="text-white">
            Detalle del plato
        </div>
    );
}