// app/user/admin/commands/page.tsx
import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Comandas',
};

export default function Commands() {
    return (
        <div>Comandas</div>
    );
}