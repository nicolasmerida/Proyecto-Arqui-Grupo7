// app/user/admin/tickets/page.tsx
import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Ticket promedio',
};

export default function Tickets() {
    return (
        <div>Ticket promedio</div>
    );
}